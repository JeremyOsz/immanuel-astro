## Learned User Preferences
- Prefers concise, command-first terminal troubleshooting with copy-pasteable commands.
- Prefers validating behavior against the hosted deployment after backend timeline changes.

## Learned Workspace Facts
- Local setup should use a Python 3.10 virtual environment; pinned dependencies fail on Python 3.13.
- API authentication uses `API_KEY` from environment configuration and requests send it as `x-api-key`.
- Hosted Vercel endpoints can be blocked by Deployment Protection unless bypass/auth is provided.
